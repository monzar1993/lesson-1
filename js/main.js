var numbers = [254, 115, 78, 25, 91, 45, 37]

for (var a = 0; a < numbers.length; a++) {
    // if (numbers[a] >= 50) {
    //     console.log(numbers[a])
    // } else {}
    var filter = numbers[a] >= 50 ? console.log(numbers[a]) : false
}
